# The scripts also declare their own deps inline (PEP 723) so `uv run scripts/<x>.py`
# resolves everything automatically — nothing to install by hand. This file is the
# plain-pip fallback for anyone not using uv.
numpy>=1.24
pandas>=2.0
scipy>=1.10
statsmodels>=0.14
arch>=6.0
matplotlib>=3.7
